import React from 'react'
const init = {
    orders: [],
    loading: false
}
export const orderReducer = (state = init, action) => {
    switch (action.type) {
        default:
            return state
    }
}
